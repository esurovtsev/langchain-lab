"""
Wrapper for MCP tool: sub_issue_write

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def sub_issue_write(
    session,
    issue_number: float,
    method: str,
    owner: str,
    repo: str,
    sub_issue_id: float,
    after_id: float | None = None,
    before_id: float | None = None,
    replace_parent: bool | None = None,
    output_file: str | None = None,
):
    """
    Add a sub-issue to a parent issue in a GitHub repository.

    Args:
        session: MCP ClientSession from connect().
        issue_number (float): The number of the parent issue
        method (str): The action to perform on a single sub-issue Options are: - 'add' - add a sub-issue to a parent issue in a GitHub repository. - 'remove' - remove a sub-issue from a parent issue in a GitHub reposito...
        owner (str): Repository owner
        repo (str): Repository name
        sub_issue_id (float): The ID of the sub-issue to add. ID is not the same as issue number
        after_id (float): The ID of the sub-issue to be prioritized after (either after_id OR before_id should be specified)
        before_id (float): The ID of the sub-issue to be prioritized before (either after_id OR before_id should be specified)
        replace_parent (bool): When true, replaces the sub-issue's current parent issue. Use with 'add' method only.
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["issue_number"] = issue_number
    args["method"] = method
    args["owner"] = owner
    args["repo"] = repo
    args["sub_issue_id"] = sub_issue_id
    if after_id is not None: args["after_id"] = after_id
    if before_id is not None: args["before_id"] = before_id
    if replace_parent is not None: args["replace_parent"] = replace_parent

    result = await session.call_tool("sub_issue_write", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
