"""
Wrapper for MCP tool: add_reply_to_pull_request_comment

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def add_reply_to_pull_request_comment(
    session,
    body: str,
    commentId: float,
    owner: str,
    pullNumber: float,
    repo: str,
    output_file: str | None = None,
):
    """
    Add a reply to an existing pull request comment. This creates a new comment that is
    linked as a reply to the specified comment.

    Args:
        session: MCP ClientSession from connect().
        body (str): The text of the reply
        commentId (float): The ID of the comment to reply to
        owner (str): Repository owner
        pullNumber (float): Pull request number
        repo (str): Repository name
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["body"] = body
    args["commentId"] = commentId
    args["owner"] = owner
    args["pullNumber"] = pullNumber
    args["repo"] = repo

    result = await session.call_tool("add_reply_to_pull_request_comment", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
