"""
Wrapper for MCP tool: assign_copilot_to_issue

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def assign_copilot_to_issue(
    session,
    issue_number: float,
    owner: str,
    repo: str,
    base_ref: str | None = None,
    custom_instructions: str | None = None,
    output_file: str | None = None,
):
    """
    Assign Copilot to a specific issue in a GitHub repository.  This tool can help with the
    following outcomes: - a Pull Request created with source code changes to resolve the
    issue   More information can be found at: - https://docs.github.com/en/copilot/using-
    github-copilot/using-copilot-coding-agent-to-work-on-tasks/about-assigning-tasks-to-
    copilot

    Args:
        session: MCP ClientSession from connect().
        issue_number (float): Issue number
        owner (str): Repository owner
        repo (str): Repository name
        base_ref (str): Git reference (e.g., branch) that the agent will start its work from. If not specified, defaults to the repository's default branch
        custom_instructions (str): Optional custom instructions to guide the agent beyond the issue body. Use this to provide additional context, constraints, or guidance that is not captured in the issue description
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["issue_number"] = issue_number
    args["owner"] = owner
    args["repo"] = repo
    if base_ref is not None: args["base_ref"] = base_ref
    if custom_instructions is not None: args["custom_instructions"] = custom_instructions

    result = await session.call_tool("assign_copilot_to_issue", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
