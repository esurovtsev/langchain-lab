"""
Wrapper for MCP tool: issue_write

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def issue_write(
    session,
    method: str,
    owner: str,
    repo: str,
    assignees: list | None = None,
    body: str | None = None,
    duplicate_of: float | None = None,
    issue_number: float | None = None,
    labels: list | None = None,
    milestone: float | None = None,
    state: str | None = None,
    state_reason: str | None = None,
    title: str | None = None,
    type: str | None = None,
    output_file: str | None = None,
):
    """
    Create a new or update an existing issue in a GitHub repository.

    Args:
        session: MCP ClientSession from connect().
        method (str): Write operation to perform on a single issue. Options are: - 'create' - creates a new issue. - 'update' - updates an existing issue.
        owner (str): Repository owner
        repo (str): Repository name
        assignees (list): Usernames to assign to this issue
        body (str): Issue body content
        duplicate_of (float): Issue number that this issue is a duplicate of. Only used when state_reason is 'duplicate'.
        issue_number (float): Issue number to update
        labels (list): Labels to apply to this issue
        milestone (float): Milestone number
        state (str): New state
        state_reason (str): Reason for the state change. Ignored unless state is changed.
        title (str): Issue title
        type (str): Type of this issue. Only use if the repository has issue types configured. Use list_issue_types tool to get valid type values for the organization. If the repository doesn't support issue types, om...
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["method"] = method
    args["owner"] = owner
    args["repo"] = repo
    if assignees is not None: args["assignees"] = assignees
    if body is not None: args["body"] = body
    if duplicate_of is not None: args["duplicate_of"] = duplicate_of
    if issue_number is not None: args["issue_number"] = issue_number
    if labels is not None: args["labels"] = labels
    if milestone is not None: args["milestone"] = milestone
    if state is not None: args["state"] = state
    if state_reason is not None: args["state_reason"] = state_reason
    if title is not None: args["title"] = title
    if type is not None: args["type"] = type

    result = await session.call_tool("issue_write", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
