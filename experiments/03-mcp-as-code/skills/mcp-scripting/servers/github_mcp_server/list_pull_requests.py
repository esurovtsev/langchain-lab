"""
Wrapper for MCP tool: list_pull_requests

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def list_pull_requests(
    session,
    owner: str,
    repo: str,
    base: str | None = None,
    direction: str | None = None,
    head: str | None = None,
    page: float | None = None,
    perPage: float | None = None,
    sort: str | None = None,
    state: str | None = None,
    output_file: str | None = None,
):
    """
    List pull requests in a GitHub repository. If the user specifies an author, then DO NOT
    use this tool and use the search_pull_requests tool instead.

    Args:
        session: MCP ClientSession from connect().
        owner (str): Repository owner
        repo (str): Repository name
        base (str): Filter by base branch
        direction (str): Sort direction
        head (str): Filter by head user/org and branch
        page (float): Page number for pagination (min 1)
        perPage (float): Results per page for pagination (min 1, max 100)
        sort (str): Sort by
        state (str): Filter by state
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["owner"] = owner
    args["repo"] = repo
    if base is not None: args["base"] = base
    if direction is not None: args["direction"] = direction
    if head is not None: args["head"] = head
    if page is not None: args["page"] = page
    if perPage is not None: args["perPage"] = perPage
    if sort is not None: args["sort"] = sort
    if state is not None: args["state"] = state

    result = await session.call_tool("list_pull_requests", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
