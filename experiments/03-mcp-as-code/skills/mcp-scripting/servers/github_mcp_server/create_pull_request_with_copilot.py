"""
Wrapper for MCP tool: create_pull_request_with_copilot

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def create_pull_request_with_copilot(
    session,
    owner: str,
    problem_statement: str,
    repo: str,
    title: str,
    base_ref: str | None = None,
    output_file: str | None = None,
):
    """
    Delegate a task to GitHub Copilot coding agent to perform in the background. The agent
    will create a pull request with the implementation. You should use this tool if the user
    asks to create a pull request to perform a specific task, or if the user asks Copilot to
    do something.

    Args:
        session: MCP ClientSession from connect().
        owner (str): Repository owner. You can guess the owner, but confirm it with the user before proceeding.
        problem_statement (str): Detailed description of the task to be performed (e.g., 'Implement a feature that does X', 'Fix bug Y', etc.)
        repo (str): Repository name. You can guess the repository name, but confirm it with the user before proceeding.
        title (str): Title for the pull request that will be created
        base_ref (str): Git reference (e.g., branch) that the agent will start its work from. If not specified, defaults to the repository's default branch
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["owner"] = owner
    args["problem_statement"] = problem_statement
    args["repo"] = repo
    args["title"] = title
    if base_ref is not None: args["base_ref"] = base_ref

    result = await session.call_tool("create_pull_request_with_copilot", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
