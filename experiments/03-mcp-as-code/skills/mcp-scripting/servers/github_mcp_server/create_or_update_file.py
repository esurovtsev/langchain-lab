"""
Wrapper for MCP tool: create_or_update_file

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def create_or_update_file(
    session,
    branch: str,
    content: str,
    message: str,
    owner: str,
    path: str,
    repo: str,
    sha: str | None = None,
    output_file: str | None = None,
):
    """
    Create or update a single file in a GitHub repository.  If updating, you should provide
    the SHA of the file you want to update. Use this tool to create or update a file in a
    GitHub repository remotely; do not use it for local file operations.  In order to obtain
    the SHA of original file version before updating, use the following git command: git
    rev-parse <branch>:<path to file>  SHA MUST be provided for existing file updates.

    Args:
        session: MCP ClientSession from connect().
        branch (str): Branch to create/update the file in
        content (str): Content of the file
        message (str): Commit message
        owner (str): Repository owner (username or organization)
        path (str): Path where to create/update the file
        repo (str): Repository name
        sha (str): The blob SHA of the file being replaced. Required if the file already exists.
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["branch"] = branch
    args["content"] = content
    args["message"] = message
    args["owner"] = owner
    args["path"] = path
    args["repo"] = repo
    if sha is not None: args["sha"] = sha

    result = await session.call_tool("create_or_update_file", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
