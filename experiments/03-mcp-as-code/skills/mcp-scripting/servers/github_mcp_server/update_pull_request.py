"""
Wrapper for MCP tool: update_pull_request

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def update_pull_request(
    session,
    owner: str,
    pullNumber: float,
    repo: str,
    base: str | None = None,
    body: str | None = None,
    draft: bool | None = None,
    maintainer_can_modify: bool | None = None,
    reviewers: list | None = None,
    state: str | None = None,
    title: str | None = None,
    output_file: str | None = None,
):
    """
    Update an existing pull request in a GitHub repository.

    Args:
        session: MCP ClientSession from connect().
        owner (str): Repository owner
        pullNumber (float): Pull request number to update
        repo (str): Repository name
        base (str): New base branch name
        body (str): New description
        draft (bool): Mark pull request as draft (true) or ready for review (false)
        maintainer_can_modify (bool): Allow maintainer edits
        reviewers (list): GitHub usernames to request reviews from
        state (str): New state
        title (str): New title
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["owner"] = owner
    args["pullNumber"] = pullNumber
    args["repo"] = repo
    if base is not None: args["base"] = base
    if body is not None: args["body"] = body
    if draft is not None: args["draft"] = draft
    if maintainer_can_modify is not None: args["maintainer_can_modify"] = maintainer_can_modify
    if reviewers is not None: args["reviewers"] = reviewers
    if state is not None: args["state"] = state
    if title is not None: args["title"] = title

    result = await session.call_tool("update_pull_request", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
