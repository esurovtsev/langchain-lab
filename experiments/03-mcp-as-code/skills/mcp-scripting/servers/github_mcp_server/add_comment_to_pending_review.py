"""
Wrapper for MCP tool: add_comment_to_pending_review

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def add_comment_to_pending_review(
    session,
    body: str,
    owner: str,
    path: str,
    pullNumber: float,
    repo: str,
    subjectType: str,
    line: float | None = None,
    side: str | None = None,
    startLine: float | None = None,
    startSide: str | None = None,
    output_file: str | None = None,
):
    """
    Add review comment to the requester's latest pending pull request review. A pending
    review needs to already exist to call this (check with the user if not sure).

    Args:
        session: MCP ClientSession from connect().
        body (str): The text of the review comment
        owner (str): Repository owner
        path (str): The relative path to the file that necessitates a comment
        pullNumber (float): Pull request number
        repo (str): Repository name
        subjectType (str): The level at which the comment is targeted
        line (float): The line of the blob in the pull request diff that the comment applies to. For multi-line comments, the last line of the range
        side (str): The side of the diff to comment on. LEFT indicates the previous state, RIGHT indicates the new state
        startLine (float): For multi-line comments, the first line of the range that the comment applies to
        startSide (str): For multi-line comments, the starting side of the diff that the comment applies to. LEFT indicates the previous state, RIGHT indicates the new state
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["body"] = body
    args["owner"] = owner
    args["path"] = path
    args["pullNumber"] = pullNumber
    args["repo"] = repo
    args["subjectType"] = subjectType
    if line is not None: args["line"] = line
    if side is not None: args["side"] = side
    if startLine is not None: args["startLine"] = startLine
    if startSide is not None: args["startSide"] = startSide

    result = await session.call_tool("add_comment_to_pending_review", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
