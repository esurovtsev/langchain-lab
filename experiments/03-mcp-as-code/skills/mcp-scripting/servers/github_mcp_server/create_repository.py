"""
Wrapper for MCP tool: create_repository

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def create_repository(
    session,
    name: str,
    autoInit: bool | None = None,
    description: str | None = None,
    organization: str | None = None,
    private: bool | None = None,
    output_file: str | None = None,
):
    """
    Create a new GitHub repository in your account or specified organization

    Args:
        session: MCP ClientSession from connect().
        name (str): Repository name
        autoInit (bool): Initialize with README
        description (str): Repository description
        organization (str): Organization to create the repository in (omit to create in your personal account)
        private (bool): Whether repo should be private
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["name"] = name
    if autoInit is not None: args["autoInit"] = autoInit
    if description is not None: args["description"] = description
    if organization is not None: args["organization"] = organization
    if private is not None: args["private"] = private

    result = await session.call_tool("create_repository", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
