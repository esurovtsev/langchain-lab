"""
Wrapper for MCP tool: pull_request_review_write

Auto-generated by generator.py — do not edit manually.
Re-generate by running: python3 generator.py github-mcp-server
"""


async def pull_request_review_write(
    session,
    method: str,
    owner: str,
    pullNumber: float,
    repo: str,
    body: str | None = None,
    commitID: str | None = None,
    event: str | None = None,
    output_file: str | None = None,
):
    """
    Create and/or submit, delete review of a pull request.  Available methods: - create:
    Create a new review of a pull request. If "event" parameter is provided, the review is
    submitted. If "event" is omitted, a pending review is created. - submit_pending: Submit
    an existing pending review of a pull request. This requires that a pending review exists
    for the current user on the specified pull request. The "body" and "event" parameters
    are used when submitting the review. - delete_pending: Delete an existing pending review
    of a pull request. This requires that a pending review exists for the current user on
    the specified pull request.

    Args:
        session: MCP ClientSession from connect().
        method (str): The write operation to perform on pull request review.
        owner (str): Repository owner
        pullNumber (float): Pull request number
        repo (str): Repository name
        body (str): Review comment text
        commitID (str): SHA of commit to review
        event (str): Review action to perform.
        output_file (str | None): If set, write full result to this file and return a summary.

    Returns:
        str: Tool result text. If output_file is set, returns a short summary instead.
    """
    args = {}
    args["method"] = method
    args["owner"] = owner
    args["pullNumber"] = pullNumber
    args["repo"] = repo
    if body is not None: args["body"] = body
    if commitID is not None: args["commitID"] = commitID
    if event is not None: args["event"] = event

    result = await session.call_tool("pull_request_review_write", arguments=args)

    # Extract text from result content
    texts = []
    for content in result.content:
        if hasattr(content, "text"):
            texts.append(content.text)
        else:
            texts.append(str(content))
    full_text = "\n".join(texts)

    if output_file:
        with open(output_file, "w") as f:
            f.write(full_text)
        lines = full_text.count("\n")
        return f"Wrote {len(full_text)} chars ({lines} lines) to {output_file}"

    return full_text
